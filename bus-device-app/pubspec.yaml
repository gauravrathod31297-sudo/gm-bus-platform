name: gm_bus_device
description: GM Bus Device App
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  geolocator: ^13.0.1
  socket_io_client: ^3.0.0
  permission_handler: ^11.3.1
  shared_preferences: ^2.2.2
  http: ^1.1.2
  provider: ^6.1.1
  flutter_tts: ^4.0.2

flutter:
  uses-material-design: true

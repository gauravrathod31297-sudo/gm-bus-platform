name: gm_user
description: GM Bus User App
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  socket_io_client: ^2.0.3+1
  http: ^1.1.2
  provider: ^6.1.1
  flutter_map: ^6.1.0
  latlong2: ^0.9.0

flutter:
  uses-material-design: true
